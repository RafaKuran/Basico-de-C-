using System;
					
public class Program
{
	public static void Main()
	{
		double salario, reajuste;
		
		Console.Write("Qual o valor do salário: ");
		salario = Convert.ToDouble(Console.ReadLine());
		Console.Write("De quanto será o reajuste: ");
		reajuste = Convert.ToDouble(Console.ReadLine());
		
		salarioFinal = salario + (salario * (reajuste / 100));
		
		Console.Write("O salário reajustado será de R$ {0}", salarioFinal.ToString("N2"));
	}
}
